# Cross-stack tests

Put browser or end-to-end tests here only when the main user journey needs them. Backend tests live in `backend/tests`; frontend unit tests should live beside frontend source files.
